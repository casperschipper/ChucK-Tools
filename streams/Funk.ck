public class Funk {
    // an object that acts like a function. all methods are virtual...
    fun float x(int arg) {
        return x(arg $ float);
    }
    
    fun float x(float arg) {
        return 0.0;
    }
}

